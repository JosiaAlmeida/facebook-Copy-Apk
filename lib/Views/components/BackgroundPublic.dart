import 'package:flutter/material.dart';

class BackgroundPublic extends StatelessWidget {
  const BackgroundPublic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}