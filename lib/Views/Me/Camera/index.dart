import 'package:flutter/material.dart';

class PhotoToFeed extends StatefulWidget {
  PhotoToFeed({Key? key}) : super(key: key);

  @override
  _PhotoToFeedState createState() => _PhotoToFeedState();
}

class _PhotoToFeedState extends State<PhotoToFeed> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}